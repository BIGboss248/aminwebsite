services:
  nextjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: nextjs-app:latest
    container_name: nextjs-standalone-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
